#include "shell.h"

/**
 * main - #include "main.h"
 *
 * Return: always 0
 */
int main(__attribute__((__unused__)) int argc, char **argv)
{
	char input[BUF_SIZE];
	char* args[CHAR_SIZE];

	while (1)
	{
		shell_prompt();

		
	}
	return (0);
}
